# Forzar legacy peer dependencies para evitar conflictos
legacy-peer-deps=true

# No forzar strict peer dependencies
strict-peer-dependencies=false

# Usar la última versión disponible al instalar
save-exact=false

# Preferir versiones estables
save-prefix=^

# No generar package-lock.json con cada install
package-lock=false