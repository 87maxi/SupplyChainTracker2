/**
 * @deprecated Esta funcionalidad se ha movido a web/src/app/admin/users/page.tsx
 * Este archivo se mantiene por compatibilidad temporal y será eliminado en una futura versión.
 * Todos los cambios y mejoras deben hacerse en la nueva ubicación.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-function
export default function RoleManager() {
  // Función vacía para evitar errores de importación
}